package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Google Calculator Material 3 Palette
val GoogleCalcBackground = Color(0xFF1A1C1E)
val GoogleCalcSurface = Color(0xFF1A1C1E)

// Number keys (0-9, .)
val GoogleCalcNumberBg = Color(0xFF2E3033)
val GoogleCalcNumberText = Color(0xFFE2E2E6)

// Function keys (AC / C, +/-, ^)
val GoogleCalcFunctionBg = Color(0xFF43474E)
val GoogleCalcFunctionText = Color(0xFFE2E2E6)

// Operator keys (÷, ×, -, +)
val GoogleCalcOperatorBg = Color(0xFF3F485B)
val GoogleCalcOperatorText = Color(0xFFD6E3FF)
val GoogleCalcOperatorActiveBg = Color(0xFFD6E3FF)
val GoogleCalcOperatorActiveText = Color(0xFF003258)

// Equals key (=) - iconic vibrant pill
val GoogleCalcEqualsBg = Color(0xFFD6E3FF)
val GoogleCalcEqualsText = Color(0xFF001B3D)

// Formula / secondary display
val GoogleCalcFormulaText = Color(0xFF919194)

// Backwards compatibility tokens
val CalcBackgroundDark = GoogleCalcBackground
val CalcNumberButtonDark = GoogleCalcNumberBg
val CalcNumberTextDark = GoogleCalcNumberText
val CalcFunctionButtonDark = GoogleCalcFunctionBg
val CalcFunctionTextDark = GoogleCalcFunctionText
val CalcOperatorButton = GoogleCalcOperatorBg
val CalcOperatorText = GoogleCalcOperatorText
val CalcOperatorActiveBg = GoogleCalcOperatorActiveBg
val CalcOperatorActiveText = GoogleCalcOperatorActiveText
